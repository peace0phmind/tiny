const formLayout = [ [ {
  prop : "name"
}, {
  prop : "username"
} ], [ {
  prop : "password", renderTo: 'input_password'
}, {
  prop : "newPassword", renderTo: 'input_password'
} ], [ {
  prop : "phone"
}, {
  prop : "email"
} ], [ {
  prop : "department"
}, {
  prop : "sex"
} ], [ {
  prop : "birthday"
}, {
  prop : "enabled"
} ] ]

export default formLayout
